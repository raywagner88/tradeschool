// Configure your import map in config/importmap.rb. Read more: https://github.com/rails/importmap-rails
// Note: This file is loaded via Vite, so importmap-style imports won't work here.
// If you need Turbo or Stimulus, install them via npm and import them properly.

// Turbo Rails (if needed, install via: npm install @hotwired/turbo-rails)
// import "@hotwired/turbo-rails"

// Stimulus controllers (if needed, import them directly with relative paths)
// import "./controllers/example_controller.js"
